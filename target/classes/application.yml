server:
  port: ${PORT:${SERVER_PORT:8081}}
spring:
  application:
    name: tri-polymer-online
  devtools:
    add-properties: false

logging:
  level:
    web: DEBUG
    root: INFO

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:hsqldb:mem:tri_polymer_online
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.HSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
