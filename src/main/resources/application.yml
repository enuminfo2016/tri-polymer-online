server:
  port: ${PORT:${SERVER_PORT:8081}}
spring:
  application:
    name: tri-polymer-online
  on-profile: stage
  devtools:
    add-properties: false

logging:
  level:
    web: DEBUG
    root: INFO

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:hsqldb:mem:tp_online
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.HSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:mysql://localhost:3306/tp_online
    username: root
    password: 
  jpa:
    show-sql: false
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      ddl-auto: update

---
spring:
  config:
    activate:
      on-profile: stage
  datasource:
    url: ${JDBC_DATABASE_URL}
    username: ${JDBC_DATABASE_USERNAME}
    password: ${JDBC_DATABASE_PASSWORD}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
